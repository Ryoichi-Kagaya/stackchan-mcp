# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/
.pytest_cache/

# Environment / secrets
.env
.env.local
*.local
AGENTS.local.md
CLAUDE.md

# Captures (user photos)
captures/
*.jpg
!docs/**/*.jpg

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Build artifacts
*.zip
build/
dist/

# ESP-IDF / firmware build artifacts
firmware/build/
firmware/managed_components/
firmware/dependencies.lock
firmware/sdkconfig
firmware/sdkconfig.old
firmware/sdkconfig.defaults.local
firmware/main/boards/stackchan/avatar_images.local.cc
firmware/main/boards/stackchan/avatar_images.local.h
firmware/nvs_config.csv
firmware/nvs_new.bin

# NVS secrets (WiFi credentials, websocket token) — real values, never commit
firmware/nvs_config.env

# Gateway runtime logs
gateway/gateway.log

# uv lockfile is OK to commit
# (allow-listed by absence of uv.lock entry above)
.claude/
